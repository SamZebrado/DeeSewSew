# 叠绣绣 DeeSewSew — human showcase plan

Prepared 2026-09-08 against `e35384af9541d754c6161650509a68a3d9a16d39` in the isolated showcase worktree. This is a recording plan, not a finished video or release approval. No browser was launched and no new media was generated for this document.

## Authority and truthful scope

The Captain Sam parallel-execution steer assigns this bounded preparation task; the main integrator owns release decisions. The local README, `docs/FLOWER-POLISH-PLAN.md`, `src/main.ts`, and existing release evidence were inspected. The historical plan's pending-publication paragraph is superseded by `review/flower-20260908/public-smoke.json`, which records the above SHA, matching asset, successful public interaction and offline reload. That is recorded evidence, not a fresh online check. Notion synchronization remains with the main integrator; this worker did not edit or independently reinterpret its status.

At this baseline it is accurate to demonstrate a browser embroidery studio, real front/back surface routing, live slack and inertia, tip/eye passage and directional tightening, user-controlled rotation, custom colors, undo/redo, local saving, JSON backup, Chinese/English UI and the existing front-only four-petal flower guide. Its reverse is honest connecting thread, **not a heart**. Do not describe the approximation as physically accurate volumetric silk or call an emulated touch viewport a tested physical tablet.

The accepted tulip-front/soft-heart-back guide, explicit thread starts, cut-thread and Ctrl-click cutting are **Workstream A, not features of this baseline**. PNG export is a separate parallel package. Do not put any of these in a public demo until the main integrator verifies their integrated release. Do not show the rejected triangle-flower diagnostic, preparation runs, synthetic Pattern Lab artwork or experimental petting/audio/3D work as shipping functionality.

## A 34-second human-recorded sequence

Record real pointer actions at normal speed, with stitch motion enabled and an ordinary camera angle. Prepare a dedicated disposable browser profile with no personal artwork; never clear Captain Sam's active piece. Keep a JSON backup of the demo piece. Start from a manually made, nearly finished four-petal flower, not a blank canvas that implausibly completes in seconds. The opening caption discloses this setup.

| Time | Human action and framing | Minimal caption |
| --- | --- | --- |
| 0–4 s | Hold the near-complete flower in a wide studio view, with Chinese title visible. | 叠绣绣 DeeSewSew · 接着绣一朵小花 / Finishing a flower |
| 4–13 s | Move the needle along a visibly curved path to leave enough slack; make the last few real guide punctures, pausing between them. Keep the entire loose thread in frame. | 移针、落针，线慢慢收紧 / Move, puncture, watch the thread tighten |
| 13–18 s | Finish and exit the guide; leave the result still briefly. Undo once and redo once, allowing each state to settle. | 可以慢慢改 / Room to change your mind |
| 18–26 s | Shift-drag or rim-drag gently, release at an intermediate angle, pause to show it stays there, then use the actual back button. Do not cut away during the rotation. | 正反面都是真实走线 / A real thread path on both faces |
| 26–30 s | Hold the back long enough to inspect its actual connecting lines; return to front using the UI. | 背面是连接线，不是第二幅图案 / The reverse shows the connecting thread |
| 30–34 s | Rest on the completed front and title. No modal or download dialog obscures the hoop. | 浏览器里的小小数字刺绣工作室 / A tiny embroidery studio in your browser |

Rehearse the 4–13 s section. If the remaining stitches cannot honestly fit, extend the take to at most 40 seconds or start closer to completion; do not speed up the pulling animation or imply the whole flower took 34 seconds. Keep one uninterrupted take for the needle/tension and rotation sections. A static image cannot establish those behaviors.

Prefer quiet, silent footage with optional captions. Music and SFX are not complete product features; add no unlicensed soundtrack or artificial needle sounds suggesting otherwise. Record at a fixed viewport that fits the full studio (the existing 1440 × 1239 desktop image is a framing reference), and preserve the original frame rate. Do not fabricate slow-motion frames, replace thread paths, or composite a more attractive reverse.

## Existing static candidates, inspected visually

Paths below are relative to the main repository, not this isolated worktree. All are existing **automated evidence**, not human-recorded promotional media. They are local, ignored artifacts and are not usable public README image links. They are references for selecting or recapturing final media; do not copy the entire review directory into Git.

| Priority / use | Existing file under `review/flower-20260908/` | What is actually visible / caveat |
| --- | --- | --- |
| 1 · artwork detail | `finished-clean-front.png` | Clean four-petal front, warm wood and fabric, no tools or guides. Existing renderer output from the recorded 41-click piece, not a screenshot of an available PNG export button. Good still insert, but not proof of interaction. |
| 2 · studio hero reference | `public-smoke-front.png` | Chinese brand, cool-neutral chrome and flower together. Includes extra smoke-test stitches and a target/active-state indication; **recapture cleanly before using as the hero**, rather than digitally erasing them. |
| 3 · honest reverse reference | `public-smoke-back.png` | Same smoke-test piece's reverse and visible back label. Irregular connecting routes are real, not a second motif; don't pair it with the different clean-front piece as though they were the same artwork. Prefer a newly recorded front/back pair from one piece. |
| 4 · tutorial/context still | `production/ux-completion-features-com-69d6a-ual-UI-and-guide-at-desktop/desktop-zh.png` | Full Chinese desktop layout with guide at 4/41. Suitable for explaining the interface, not a finished-work hero; the progress and target must remain visible if used. |
| 5 · narrow layout reference | `production/ux-c1-palette-Cool-Linen-v-8d6e6--identical-artwork-at-phone/phone-B-cool-linen.png` | English phone-sized layout, long scroll, scattered test stitches and loose line crossing the title. **Recapture required for promotion**; this is an emulated viewport, not physical-phone evidence. |

`finished-provenance.json` identifies the clean artwork as 41 actual production pointer clicks, 40 segments and a renderer capture without transient arguments. `public-smoke.json` records 43 punctures for the separate smoke-test piece. Preserve that distinction in captions and provenance. Candidate ranking reflects actual inspection, not filename assumptions; none of these files is silently promoted to a new final marketing asset.

## README and release handoff

The README already uses the requested paired brand, a clear bilingual description and explicit limitations. No wording change is needed merely to consume this workstream. After a human recording is approved, place one compact poster/video link immediately after the two introduction paragraphs and before “当前功能 / Features”. Use alt text such as “叠绣绣 DeeSewSew：人工演示落针、收线和查看背面 / Human demo of stitching, tightening and reverse inspection”. Prefer a small still linking to an externally hosted release video over a large committed looping GIF. Do not add broken placeholder links now.

Before main-agent selection/publication:

- Identify the exact release SHA and verify the served build; refresh this plan if Workstream A has shipped. Record viewport, browser, input device, locale, motion settings and whether the source is a physical device or desktop screen capture.
- Use the same saved artwork for front/back comparisons; exit the guide, settle motion and move the pointer away for a clean still. Do not remove canonical stitches or anchor artifacts for appearances.
- Inspect Chinese and English title/caption rendering, browser chrome for private tabs/paths, and the whole loose-thread path for cropping. Keep the hoop the focal point.
- Keep original human footage distinct from automated QA videos and clean renderer images. Note any ordinary editorial cuts; never use cuts to hide a discontinuity.
- If the tulip/heart release is selected later, replace—not relabel—the flower sequence with its actual released start/stitch/cut actions and one honest front/back reveal. Caption prepared artwork as prepared; do not imply all 48 punctures were made in a short take. Main must first clear its visual and exact-SHA publication gates.

Merge recommendation: documentation-only preparation may be integrated independently; media selection and any public feature claim remain gated by the main integrator's current release evidence. No runtime code, existing user data, dependencies, review policy or binary assets changed.
