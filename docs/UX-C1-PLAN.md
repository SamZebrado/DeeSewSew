# UX-C1 — bounded product package

Base: `44f55c1107223fa3c7d247f862accf0b4b9a684c`. One sequential branch; preserve reviewed repair and advanced WIP. No publication authorization.

Latest direct user scope overrides the older Notion REPAIR-C-only sequence. Subsequent STEER makes travelling tension-front tightening a mandatory REPAIR-C core requirement before High review or human feel check. Pen mapping, roll, advanced physics/materials, audio and cloud remain deferred.

## Implementation sequence and contracts

0. Mandatory directional tightening (STEER): preserve exact captured cubic loose path and settled renderer cubic. Subdivide loose cubics exactly into bounded small spans, measure cumulative arc length using a fixed quadrature/sample budget, and split the settled cubic at corresponding normalized arc lengths. One monotonic smooth front travels from new/pulled end u=0 to old hole u=1; each control sample uses its arc-length coordinate, not node index or timer. Pure absolute-progress evaluation, exact p=0/p=1 branches, O(N) bounded work and no per-node state. Validate direction, unchanged far slack, near-final continuity, frame schedules, both faces, long/short/curved paths, rapid tails and motion preferences. Capture and inspect continuous real-browser video before review. The damped-string wave-speed structure motivates direction/locality only; no measured material constants or full PDE claim.

1. Rotation: reuse yaw/pitch controller; Shift-primary drag bypasses puncture hit testing. Track touch contacts independently of primary-pointer designation. Second contact cancels uncommitted stitching before centroid rotation. On fewer than two contacts freeze angle and suppress remaining contacts until all lift. Cancellation, blur and capture loss must never commit. Test mouse, real browser two-touch lifecycle, cancel and existing angle gates.
2. Locale: small zh/en keyed catalog, safe persisted explicit preference with browser-language fallback. Update text/ARIA in place, never rebuild the studio or reset canonical/view/history state. Test fallback, persistence and state invariance.
3. Files: first harden canonical parser (IDs/orders, circular bounds, finite widths and budgets). Versioned local envelope; validate before confirmation or mutation. Preserve current work on failure; successful replacement creates a coherent history boundary and uses existing fail-closed storage. Test valid roundtrip and hostile/oversized data.
4. Leaf: optional authored ordered targets with faint guide and one highlighted target. Actual puncture transactions only; monochrome color snapshot, gentle wrong-target rejection, exit keeps work. Progress is separate and reconciled with canonical history; reload must not invent stitches.
5. README: Chinese first then English in each section; truthful controls, local-only files, limits, browser evidence and Apache-2.0.

## Verification and handoff

Additional visual STEER: use Cool Linen tokens for application chrome only; preserve canvas/material colors and serif wordmark. Opaque paper-like tools, 17px panel and 11px controls. Capture exact 44f55c1 cream CSS versus candidate CSS on identical canonical artwork/view at desktop/tablet/phone, check text contrast and unchanged canvas pixels. No theme selector. Include pairs in High review; final aesthetic decision belongs to Captain Sam. Full Chinese UI remains pending in the broader localization stage; current bilingual rotation hint is covered, not misrepresented as full localization.

Run unit/typecheck/build; desktop/touch E2E, locale/files/guide regressions, responsive and production offline tests. Keep actual screenshots/video in ignored review artifacts. Bind High implementation review to exact candidate SHA and evidence. Fix blockers and re-review a new SHA. Leave playable local candidate for Captain Sam; no push/deploy or publication request.
