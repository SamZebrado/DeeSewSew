# Flower starter and needle polish — implementation contract

Base: `005489d795728d0470ac08176cad27c437782a47`. The working tree is clean; refreshed origin/main and the successful Pages run 34128748074 match this base. Existing WIP branches, preview servers and reviewer Chrome are preserved.

## Authority and sequence

The September 8 direct request and its supplement supersede the older leaf tutorial, untranslated brand and pre-publication human gate. The supplement rejects the 109-step leaf/cat construction. Publication still requires a new exact-commit High approval, passing CI and public smoke verification.

1. **Design before integration.** Develop at least three materially different small alternating-route candidates in an offline/dev-only harness. Separate intended front/back edges, executable punctures and presentation. Validate actual topology parity, bounds, zero moves, retraces and route length. Render both faces through the existing canonical renderer, with no helper overlays. Inspect normal-scale images before freezing anything. Aim for 20–40 punctures. Prefer a simple flower/heart; simplify or use an honestly disclosed attractive front-only flower if dual-face attempts are not visually convincing. No runtime search or fabricated completion strokes.
2. **Guide integration.** Freeze only the selected small route. Preserve every pre-existing stitch. Handle initial needle side/anchor explicitly without inventing an edge or flipping the camera. Version the actual pattern session contract so an old leaf session cannot silently resume as a flower; do not change artwork schemas. Test start, exact/tolerated points, undo/redo, reload, exit, re-entry, completion and real back inspection. Completion copy invites a flip without forcing it or revealing the surprise early.
3. **Copy and needle.** Chinese brand 叠绣绣, English DeeSewSew; Chinese-first paired README and requested studio descriptions. Change only existing 罂粟红 to 茜红; 樱素红 is absent. Preserve color values and stable storage keys. Increase screen-up weighting initially to 1.45, retain mirrored center-right bias, exact tip/eye and bounded pose solver. Brighten procedural metallic highlights with a narrow silver reflection, not a white silhouette; retain restrained exact-tip madder glint. No physics/topology changes.
4. **Verification.** Unit, typecheck, build, full browser, production, offline/PWA, responsive Chinese/English, complete guide lifecycle, pointer/needle quadrants and focused performance. Preserve continuous needle/passage/tension evidence. Compare needle screenshots against the baseline with the same pose. Distinguish browser emulation from real-device evidence.
5. **Release.** Commit a clean bounded candidate; package source, delta, logs, normal-scale front/back previews and hashes. Send once to the designated High Bridge conversation, recover by request ID, and require FINAL_PUBLICATION_APPROVAL: YES for that SHA. Refresh remote and fast-forward push only after approval. Verify Pages and public asset identity plus online/offline interaction. Update the existing Notion plan with actual results, not speculative completion.

## Invariants / exclusions

No mandatory flip; no canonical schema change; existing artwork survives; tip hit-testing and eye attachment remain distinct; same-frame endpoints, tip-first passage, extra free thread, NEW→OLD tension, hidden-side truth, bounded tails, reduced/off motion and rotation remain intact. No pattern library, new rendering system, material/physics controls, music, stylus features, cloud or analytics.

## Status

Design-search and pre-integration visual comparison complete: A four-petal outline (33 punctures), B five-petal outline (41), C blossom/stem (35), D simpler center petals (33), E flower/star (33), F wider soft flower (41). A–E failed the dual-face visual recognition gate; none is represented as a successful heart or star. F is the explicitly allowed front-only fallback, with honest back routing. Its 20 front edges form four broad petals; the extra puncture over the approximate 40 target preserves that outline. All candidates use actual canonical puncture generation and the existing renderer; rejected candidate snapshots remain in the local review evidence, not the product.

The fixed F route has 17 points, 41 punctures, zero zero-length moves, zero out-of-fabric points, no repeated front edges and three repeated back edges. Total normalized thread length is approximately 4.9912. A back-side start omits the initial lead-in puncture (40 total), preserving all 20 front strokes. Existing artwork is retained, including the real lead-in connector; paired cycle rotation avoids starting on the current hole. Version 1 leaf sessions are ignored without touching their stitches; the stable storage slot and artwork schema are unchanged. Guide target arrays are cached by session rather than rebuilt on pointer frames.

Brand, catalog, metallic highlight, 1.45 up-weight and guide integration implemented. Final local validation: 152 units, typecheck/build, 62 full browser, 41 production, 2 offline/PWA, four responsive evidence images and one isolated performance profile pass. The seven earlier focused browser cases include full motion-on completion, both starting sides, old-artwork retention, reload, undo/redo, exit and re-entry. Actual 41-click production completion is preserved as canonical JSON plus real front/back page captures and separate clean renderer images at the same canvas size. A continuous 20 fps contact sheet confirms upper NEW-end tightening while the lower OLD-end remains bent temporarily, then exact final settling.

Isolated 1000-segment dev profile: render p95 0.2 ms, synchronous pointer-handler p95 0.5 ms, RAF scheduling p95 16.8 ms; 3-second idle and simulated hidden periods each make zero extra renders. Historical baseline measurements were 0.5/1.4/16.8 ms respectively: no observed regression, not a controlled claim of speedup. Real-device input-to-photon latency is not measured. A new exact-SHA High approval and publication verification remain pending.
